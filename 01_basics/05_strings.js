const name = "adarsh"
const repoCount = 50

// console.log(name + repoCount + "  value"); this is not looking good so we use lower mwethod

console.log(`Hello my name is ${name} and my repo count is ${repoCount}`);